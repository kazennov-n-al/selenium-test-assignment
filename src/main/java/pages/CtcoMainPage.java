package pages;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.support.ui.WebDriverWait;

public class CtcoMainPage extends BaseCtcoPage {
    public CtcoMainPage(WebDriver driver, WebDriverWait wait) {
        super(driver, wait);
    }
}
